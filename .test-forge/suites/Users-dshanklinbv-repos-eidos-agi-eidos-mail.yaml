name: Users-dshanklinbv-repos-eidos-agi-eidos-mail
target: /Users/dshanklinbv/repos-eidos-agi/eidos-mail
target_type: repo
created_at: '2026-03-09T03:53:55Z'
steps:
- name: git status clean
  type: cli
  config:
    command: cd /Users/dshanklinbv/repos-eidos-agi/eidos-mail && git status --porcelain
    expect_exit_code: 0
  why: no uncommitted changes
- name: imports cleanly
  type: cli
  config:
    command: cd /Users/dshanklinbv/repos-eidos-agi/eidos-mail && python -c 'print("ok")'
    expect_exit_code: 0
  why: code can be loaded
- name: tests pass
  type: cli
  config:
    command: cd /Users/dshanklinbv/repos-eidos-agi/eidos-mail && python -m pytest
      -x -q 2>&1
    expect_exit_code: 0
  why: tests pass
- name: no uncommitted changes
  type: cli
  config:
    command: cd /Users/dshanklinbv/repos-eidos-agi/eidos-mail && git status --porcelain
    expect_exit_code: 0
  why: repo is clean
- name: type checks pass
  type: cli
  config:
    command: cd /Users/dshanklinbv/repos-eidos-agi/eidos-mail && python -m pyright
      --outputjson 2>&1 | head -1 || true
    expect_exit_code: 0
  why: no type errors
